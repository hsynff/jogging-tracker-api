package com.jogging.tracker.util;

/**
 * Marker interfaces for determining validation groups
 */
public interface Markers {
    interface CreateRecord {
    }

    interface CreateUser {
    }

    interface Login {
    }

    interface Register {
    }

    interface UpdateRecord {
    }

    interface UpdateUser {
    }
}
